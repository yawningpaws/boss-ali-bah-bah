<% content_for :current_user do %>
  <% if user_signed_in? %>
    <%= current_user.email %>
    <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>

     <%= render 'users/onboarding' %>
  <% end %>
<% end %>
